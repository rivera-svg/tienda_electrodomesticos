# Puerto est�ndar para el servidor Eureka
server.port=8761

# Nombre de esta aplicaci�n (servidor de descubrimiento)
spring.application.name=eureka-server

# --- Configuraci�n del Cliente Eureka (para el propio servidor) ---

# Le decimos al servidor que NO se registre a s� mismo en el registro.
eureka.client.register-with-eureka=false

# Le decimos al servidor que NO intente obtener una copia del registro de otro par.
# (En un cl�ster de Eureka, esto ser�a 'true').
eureka.client.fetch-registry=false

# --- Configuraci�n del Servidor Eureka ---

# Deshabilita el modo de auto-preservaci�n.
# Esto es �til en desarrollo para que los servicios ca�dos se eliminen
# del registro r�pidamente. En producci�n, se recomienda dejarlo en 'true'
# para proteger contra fallos de red.

eureka.server.enable-self-preservation=false